export interface ShareData { 
    shapeId: string,
    addPoints?: number[],
    points?: number[], 
    connected?: string, 
    width? : number, 
    height? : number, 
    radiusX?: number, 
    radiusY?: number 
}